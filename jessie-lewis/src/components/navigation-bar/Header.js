import React from "react";
import Logo from "../Logo";

const Header = () => {
  return (
    <header>
      {/* Logo Div */}
      <Logo height={45} />
      {/* End Of Logo Div */}
    </header>
  );
};

export default Header;
